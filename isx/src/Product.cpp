#include "Product.h"
// system headers
#include <Windows.h>
/*
Product::Product(std::string title) :Job(title) {
    
}

std::string Product::getName() {
    return mPArg->Title;
}

void Product::Start(t_UpdateCb onUpdate, LPVOID lpParam) {
    Job::Start(onUpdate, lpParam);
}
*/
    /*for (auto it = mJobs.begin(); it != mJobs.end(); ++it)
    {
        UIParams ui = Events::getCurrentUI();
        ui.setLabel(1, mJobs);
    }

    

    for (int i = 0; i <= 10; i++) {
        ui.setLabel(1, "Product1");
        ui.setLabel(2, "Yo");
        ui.setLabel(3, "--");
        ui.progress1 = i * 10;
        ui.progress2 = 100 - i * 10;
        Events::SendUIEvent(ui);
        Sleep(200);

        if (WaitForSingleObject(Events::UserCancelEvent(), 0) == WAIT_OBJECT_0) {
            Dialog1 * obj = (Dialog1 *)lpParam;
            obj->result = "Cancel";
            return 1;
        }
    }*/
